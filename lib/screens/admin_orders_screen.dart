// lib/screens/admin_orders_screen.dart

import 'package:flutter/material.dart';

// Le contenu du fichier a été remplacé par le widget écran AdminOrdersScreen
class AdminOrdersScreen extends StatelessWidget {
  const AdminOrdersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // L'AppBar sera affichée par défaut si non masquée par l'écran parent
      body: const Center(
        // Remplacez par le contenu réel des commandes plus tard
        child: Text('Écran de Gestion des Commandes (WIP)'),
      ),
    );
  }
}