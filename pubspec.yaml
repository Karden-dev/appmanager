name: wink_manager
description: Wink Express Admin Management Application.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # AJOUT: Dépendance pour la localisation (DatePickers, etc.)
  flutter_localizations:
    sdk: flutter

  # Icônes
  cupertino_icons: ^1.0.6
  
  # Services HTTP
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  
  # Utilitaires de données et UI
  provider: ^6.1.1
  collection: ^1.18.0 
  
  # CORRECTION: Mise à jour de la version pour correspondre au SDK Flutter
  intl: ^0.20.2 
  
  # Formulaires et Affichage
  flutter_spinbox: ^0.13.1 
  url_launcher: ^6.2.2
  
  # Base de données (si utilisé, conserver)
  # sqflite: ^2.3.0
  # path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  #assets:
    #- assets/images/